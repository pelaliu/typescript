const initState = {
    dataList: []
}
export default (state = initState, action) => {
    switch (action.type) {
      case 'DELETE':
        return state.split(index,1);
      default:
        return state
    }
  }